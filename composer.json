{
    "name": "Odoo/carpooling-platform",
    "description": "Enterprise Carpooling Platform â€” Core PHP MVC",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if(!is_dir('storage/logs')) mkdir('storage/logs', 0755, true);\"",
            "@php -r \"if(!is_dir('storage/temp')) mkdir('storage/temp', 0755, true);\"",
            "@php -r \"if(!is_dir('public/assets/uploads')) mkdir('public/assets/uploads', 0755, true);\""
        ]
    }
}
