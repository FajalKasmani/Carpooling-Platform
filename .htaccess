# Apache rewrite engine configuration to redirect root requests to public/ folder
<IfModule mod_rewrite.c>
    RewriteEngine Off
    RewriteRule ^$ public/ [L]
    RewriteRule ^((?!public/).*)$ public/$1 [L]
</IfModule>
